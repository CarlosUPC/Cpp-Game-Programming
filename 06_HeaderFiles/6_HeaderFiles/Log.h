#ifndef _LOG_KEY
#define _LOG_KEY

void InitLog();
void Log(const char*);

struct Player {}; //Cuidado con la declaracion de structs ya que no se pueden declarar mas de 1 vez (redefinition error)
#endif


